{
  "name": "cat-personality-codes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:create": "wrangler d1 create cat-personality-codes-db",
    "db:migrate": "wrangler d1 execute cat-personality-codes-db --file=./schema.sql",
    "db:seed": "wrangler d1 execute cat-personality-codes-db --file=./seed.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
